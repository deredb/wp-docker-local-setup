# basic Wordpress docker image setup 

Foobar is a Python library for dealing with word pluralization.

## Installation

In working directory, 

```bash
git docker build -t wtest .
docker-compose up
```